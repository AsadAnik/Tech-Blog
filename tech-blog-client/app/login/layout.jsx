export default function loginLayout({ children }) {
    return (
        <section>
            {children}
            <footer>
                Powered by next application here.
            </footer>
        </section>
    );
}