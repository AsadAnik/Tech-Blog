export default function Layout({ children }) {
    return (
        <section>
            {children}
            <footer>
                Powered by next application here.
            </footer>
        </section>
    );
}